{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Equipe</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Designation</th>
                <td>{{ entity.designation }}</td>
            </tr>
            <tr>
                <th>Adresselocal</th>
                <td>{{ entity.adresselocal }}</td>
            </tr>
            <tr>
                <th>Typeequipe</th>
                <td>{{ entity.typeequipe }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('equipe') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('equipe_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
