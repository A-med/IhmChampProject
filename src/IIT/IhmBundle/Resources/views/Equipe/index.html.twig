{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Equipe list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Designation</th>
                <th>Adresselocal</th>
                <th>Typeequipe</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('equipe_show', { 'id': entity.id }) }}">{{ entity.designation }}</a></td>
                <td>{{ entity.adresselocal }}</td>
                <td>{{ entity.typeequipe }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('equipe_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('equipe_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('equipe_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
