{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Stade list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Designation</th>
                <th>Pays</th>
                <th>Adresse</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('stade_show', { 'id': entity.id }) }}">{{ entity.designation }}</a></td>
                <td>{{ entity.pays }}</td>
                <td>{{ entity.adresse }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('stade_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('stade_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('stade_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
