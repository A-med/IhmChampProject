{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Heure creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('heure') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
